
  # Website Redesign with Blue Theme

  This is a code bundle for Website Redesign with Blue Theme. The original project is available at https://www.figma.com/design/VglExeAbintjNcLXIGNt57/Website-Redesign-with-Blue-Theme.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  